# Plan: CLAUDE.md refinements

## Changes
1. Remove `Model` column from all skill tables — model is an implementation detail in each SKILL.md
2. Add core principle about agents: agents can be created as workflow bundles, but all outputs belong to skills
3. Add example run section showing a realistic flow — research → DD midway → external model input → consolidate

## File
- `CLAUDE.md` — rewrite Skills section (no model column), add agent principle, add example run
